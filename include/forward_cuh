struct ForwardArg{

    using index_t = int64_t;

    void* __restrict__ Q;
    void* __restrict__ K;
    void* __restrict__ V;
    void* __restrict__ O;

    const index_t batch_stride;
    const index_t seq_stride;
    const index_t head_stride;
    

    const int n_Qblocks;
    const int n_KVblocks;

    const int batch = blockIdx.z;
    const int head = blockIdx.y;
    const int Qblock = blockIdx.x;
};